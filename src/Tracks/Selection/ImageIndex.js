import React from 'react';

function ImageIndex(props) {
    return (
        <div>
            <h2>IMAGEINDEX: I SHOW EVERY IMAGE SO USERS CAN FAVORITE</h2>
        </div>
    );
}

export default ImageIndex;