import React from 'react';

function TrackNav(props) {
    return (
        <div>
            <h2>I NAVIGATE TO PAGES THAT HELP SELECT WINNERS {"(discussion/voting etc.)"}</h2>
        </div>
    );
}

export default TrackNav;